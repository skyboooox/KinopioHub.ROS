# Examples

本目录只保留可直接用于配置校验的最小示例。

- `config.minimal.yaml`：最小配置，用于 `--dry-run` 和配置解析校验。
- 根目录 `config.example.yaml`：完整公开示例，含显式 allowlist service request-reply；复制为未跟踪的 `config.yaml` 后再填真实 NATS server。
